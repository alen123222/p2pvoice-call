# 重构与检查记录

本次修改覆盖 Flutter 客户端、WebSocket 信令服务和 Android 前台服务。
保留语音呼叫、接听、拒绝、挂断、静音、扬声器、STUN/TURN、身份与头像、
自定义背景、四种配色、中英法语言和已有偏好存储键。

## 修复的问题

| 原问题 | 现在的处理 |
| --- | --- |
| 首页约 2,940 行，UI、状态和服务事件混在一起 | 拆为会话控制器、首页、通话页、设置页和资料面板 |
| 未接听的 offer 可以触发媒体采集 | 只接受当前对端、当前方向且已明确接听的 offer |
| 初始化连接会清空提前收到的 ICE | 候选保留至 remote description 就绪再提交；队列有上限 |
| 挂断时仍在等待权限或媒体初始化 | 会话代次使晚到结果失效；晚到的媒体资源单独释放 |
| 连点拨号或重复 accepted 导致多次协商 | 通话占线与协商标志阻止重复操作 |
| 来电 / 接听后没有完整超时覆盖 | 呼叫、来电和协商均设超时；断网允许短暂恢复 |
| ICE connected/completed 重复启动计时 | 状态去重，通话时长使用 Stopwatch |
| 扬声器状态与下一通电话不一致 | 挂断等待输出切换完成，并恢复输出路由 |
| 心跳只发送，不检测响应 | pong 超时离线重连；注册也有超时 |
| 相同 ID 冲突导致双方反复抢登录 | 冲突与注册失败停止自动重试，可手动恢复 |
| TURN 凭据久置后失效 | 按返回的 TTL 提前刷新；过期凭据不用于新通话 |
| 字符串猜测直连并显示错误宣传文案 | 根据选中 candidate pair 的双方候选判断直连 / TURN |
| 服务端接受 null、错误类型 userId 时可能异常 | 消息对象、ID、SDP 和 ICE 校验 |
| 未注册连接能查询用户和 TURN | 查询和路由都校验当前 socket 的注册所有权 |
| 身份替换后旧 socket 可能影响在线映射 | 重命名、注销和路由均检查所有权 |
| 设置与头像面板挡住来电 | 来电优先收起编辑界面 |
| 首页固定布局、键盘遮挡、装饰层过多 | 自适应分栏、可滚动布局、系统输入框与统一实体卡片 |
| 前台服务失败后重试全部类型 | 显式使用所需类型，失败清理；补充系统超时处理 |

## 验证范围

- 33 项 Flutter 测试通过：状态机、媒体取消、音频复位、真实本地 WebSocket、
  身份和 URL 校验、语言、首页交互、设置来电优先，以及三种屏幕尺寸下三种语言的 200% 字号布局。
- Node 集成测试通过：错误消息、鉴权、TURN、全部通话信令路由、发送方身份、离线、重命名、冲突、心跳、注销。
- Flutter 静态分析及 Android 调试 APK 构建作为交付检查。
- `artifacts/home-dark.png` 和 `home-light.png` 是应用 widget 渲染截图，使用测试会话状态。

## 尚需真机验收

本机没有连接的 Android 设备、已配置的模拟器或 iOS 构建环境。
自动测试不替代以下检查：两机双向音频、拒绝权限后恢复、蓝牙/扬声器路由、
锁屏接听、切到后台、Wi-Fi/蜂窝网络切换、强制 TURN 中继和持续通话。
未连接或部署线上信令服务器；默认服务地址保持原项目配置。

Android 的 dataSync 前台服务不是无限在线机制：Android 15 起存在时长限制，
现已增加超时停止处理，但没有新增推送唤醒架构。
参考 [Android 前台服务超时规则](https://developer.android.com/develop/background-work/services/fgs/timeout)。
麦克风前台服务仍受前台可见性和权限限制，参考
[Android 前台服务启动限制](https://developer.android.com/develop/background-work/services/fgs/restrictions-bg-start)。
iOS 未新增 PushKit / CallKit，不能保证系统挂起或杀死后的来电送达。

发布前仍需配置自己的 WSS、信令令牌、相互匹配的 TURN_SECRET / coturn 密钥，
以及 Android 发布签名。当前 APK 是开发调试包。
构建工具提示 flutter_webrtc 的 Kotlin Gradle 插件未来兼容性问题；本次未升级依赖，
避免将通话库升级与应用重构混为同一次变更。
